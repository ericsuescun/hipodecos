IF M.NUMERO <> NUMERO
APPEND BLANK
&&APPEND MEMO DESCR FROM TITULO.TXT
&&APPEND MEMO DESCR FROM PREVIAS.TXT
MODIFY MEMO DESCR

DO MUEVEVAR


DO CASE
   
   CASE CLAVE = "H"
   use matricula
   append blank
&&   replace nombre with m.nombre, apellido with m.apellido, clave with m.clave, entidad with m.entidad
   replace numero with m.numero
   replace fecharec with DATE()
   use Hbio3
   go bottom

   CASE CLAVE = "C"
   use matricula
   append blank
&&   replace nombre with m.nombre, apellido with m.apellido, clave with m.clave, entidad with m.entidad
   replace numero with m.numero
   replace fecharec with DATE()
   use Cbio3
   go bottom
ENDCASE

_CUROBJ = OBJNUM(M.SIGUIENTE)
ELSE
DO AVISO
DO GUARDAR
DO SINAVISO
_CUROBJ = OBJNUM(M.SALIDA)
ENDIF
