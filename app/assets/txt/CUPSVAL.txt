SELECT Cupsval.clinica, Cupsval.codval1, COUNT(Cupsval.codval1), SUM(Cupsval.saldo);
 FROM Cupsval;
 GROUP BY Cupsval.codval1;
 INTO CURSOR Query
CREATE REPORT cupsval.frx FROM Query WIDTH 1280 COLUMN NOOVERWRITE
REPORT FORM cupsval.frx PREVIEW  &&to print noconsole
CLOSE DATA
RETURN
ENDIF

   DEACTIVATE WINDOW COREGIR
RELEASE WINDOW COREGIR

CLOSE DATA

&&RETURN

use cupsval
