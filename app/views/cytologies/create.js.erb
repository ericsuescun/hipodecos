$(document).on('page:load', refresh());

function refresh() {

	$('div.cytologies').remove();
	$('div.cytology').append('<%= j render "inform/cytologies", inform: @inform, edit_cytology_form: nil %>');

	
	
	setTimeout(function() {
		$('div.alert-growl').remove();
	}, 3000)
}