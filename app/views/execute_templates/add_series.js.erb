<% if @inform.errors.empty? %>
	$(document).on('page:load', refresh2());
	function refresh2() {
	  $('div.slides').remove();
	  $('div.slide').append('<%= j render "informs/slides", inform: @inform %>');
	  $('div.fast-bar').append('<%= j render "ok-message", message: "creado" %>');
	  setTimeout(function() {
	  	$('div.alert-growl').remove();
	  }, 3000)
	}
<% end %>