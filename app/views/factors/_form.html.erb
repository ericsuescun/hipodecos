<div class="container">
  <div class="row">
    <div class="col-12 col-lg-4">
      <%= form_with(model: factor, local: true) do |form| %>

        <div class="form-group">
          <%= form.label "Factor: 1 + margen" %>
          <%= form.text_field :factor, class: "form-control input" %>
        </div>

        <div class="form-group">
          <%= form.label "Descripción" %>
          <%= form.text_area :description, class: "form-control input" %>
        </div>

        <div class="actions">
          <%= form.submit %>
        </div>
      <% end %>
    </div>
  </div>
</div>