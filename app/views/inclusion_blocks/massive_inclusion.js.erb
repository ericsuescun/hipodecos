
$(document).on('page:load', refresh2());
function refresh2() {

  $('div.inclusion-blocks').remove();
  $('div.inclusion-block').append('<%= j render "inclusion_blocks/control-board2", inclusion: @inclusion %>');

  
}
