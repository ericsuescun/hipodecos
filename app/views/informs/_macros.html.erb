<h1>Macro para <%= @inform.tag_code %></h1>
<div class="row">
  <div class="col-12 col-lg-8">
    <div class="row">
      <% @inform.macros.each do |macro| %>
        <div class="col-12 col-lg-3">
          <div class="card text-center mt-3">
            <div class="card-header">
              <strong>Macro</strong>
            </div>
            <div class="card-body">
              <h5 class="card-title">Descripción</h5>
              <p class="card-text"><%= macro.description %></p>
              <%= link_to "Editar", edit_macro_path(macro), class: 'btn btn-primary' %>
              <%= link_to 'Borrar', macro, method: :delete, data: { confirm: '¿Confirmas la operación?' }, class: 'btn btn-primary' %>
            </div>
            <div class="card-footer text-muted">
              <p>Creada el <%= macro.created_at %> por <%= User.find(macro.user_id).email %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>      
  </div>
  <div class="col-12 col-lg-3 mx-auto">
    <%= form_for [@inform, @inform.macros.build] do |form| %>
      <div class="form-group">
        <%= form.label "Descripción" %>
        <%= form.text_area :description, class: "form-control form-control-lg input" %>
      </div>
      
      <div class="actions">
        <%= form.submit "Agregar", class: "btn btn-primary btn-block" %>
      </div>

    <% end %>
  </div>
</div>