<%= form_for [imageable, imageable.pictures.build] do |form| %>
  
  <div class="form-group">
    <%= form.file_field :name, class: "btn btn-primary my-3" %>
  </div>

  <div class="form-group">
    <%= form.label "Descripción" %>
    <%= form.text_area :description, class: "form-control input" %>
  </div>

  <div class="actions">
    <%= form.submit "Cargar imagen", class: "btn btn-primary my-3" %>
  </div>

<% end %>
