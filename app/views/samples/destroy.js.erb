<% if @inform.errors.empty? %>
	$(document).on('page:load', refresh2());
	// $(document).on("click", 'a[data-remote=true]', refresh2());

	function refresh2() {
	  // $('div.recipient-' + @recipient.tag).remove();
	  console.log('deleteing sample!');
	  $('div.recipient-' + '<%= @recipient.tag %>').remove();
	  $('div.elements-in-for-' + '<%= @recipient.tag %>').append('<%= j render "informs/samples_per_rec", inform: @inform, recipient: @recipient %>');
	  $('div.fast-bar').append('<%= j render "ok-destroy" %>');
	  setTimeout(function() {
	  	$('div.alert-growl').remove();
	  }, 3000)
	}
<% end %>