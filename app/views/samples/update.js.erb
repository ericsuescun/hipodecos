<% if @inform.errors.empty? %>
	$(document).on('page:load', refresh2());
	function refresh2() {
	  console.log('Recipiente creado!');
	  $('div.recipients').remove();
	  $('div.recipient').append('<%= j render "recipients/recipients" %>');
	  $('body').prepend('<%= j render "ok-create" %>');
	}
<% end %>
