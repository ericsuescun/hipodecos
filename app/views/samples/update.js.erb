<% if @sample.errors.empty? %>
	$(document).on('page:load', refreshRecipients());

	function refreshRecipients() {
		
		console.log('Muestra en edición');
		$('div.recipients').remove();
		$('div.recipient').append('<%= j render "informs/recipients", inform: @inform, automatics: @automatics, edit_sample_tag: "", edit_recipient_tag: "" %>');

		$('div.slides').remove();
		$('div.slide').append('<%= j render "informs/slides", inform: @inform %>');

		$('div.fast-bar').append('<%= j render "ok-update" %>');
		setTimeout(function() {
			$('div.alert-growl').remove();
		}, 3000)
		// $('body').append('<%= j render "ok-create" %>');
		// $('input#organ-1').focus();
	}

<% end %>