<%= form_with(model: utility_file, local: true) do |form| %>
  <% if utility_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utility_file.errors.count, "error") %> prohibited this utility_file from being saved:</h2>

      <ul>
      <% utility_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :filepath %>
    <%= form.text_field :filepath %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
